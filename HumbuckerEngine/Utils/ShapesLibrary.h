#pragma once
#include <vector>

struct Square
{
	static std::vector<float> Vertices;
	static std::vector<unsigned int> Indices;
};

struct Cube
{
	static std::vector<float> Vertices;
	static std::vector<unsigned int> Indices;
};